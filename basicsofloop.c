#include<stdio.h>
int main(){
    int n;
    printf("Enter the number : ");
    scanf("%d",&n);

    for (int i=0; i<=n; i++){    //(i++ equle to (i=i+1) is same)
        printf("vikash\n");
    }
    return 0;
}